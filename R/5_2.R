# instructions <- Map(na.omit, Map(as.integer,strsplit(input[11:length(input)], "")))
# crates
# keep_seq <- function(x) x[seq(from = 2, to = length(x), by = 4)]
# 
# Map(keep_seq, strsplit(input[1:8], "")) 
